#include <iostream>
#include <cmath>
#include <math.h>
using namespace std;

// int calculateBitLength(int num){
//     if (num == 0) {
//         return 1; // Special case: the number 0 requires 1 bit to represent
//     }
//     return floor(log2(num)) + 1;
// }

int main()
{

    // cout<<calculateBitLength(8);

    //  Problem Solving of Bit Manipulation....
    // int number = 10,ans = 0,i=0;
    // //  we need to find the binary digit for the particular decimal number.

    // while(number){

    // //  int bitNumber = number & 1;

    // // ans = (bitNumber * pow(10,i)) + ans;

    // // number = number >> 1;
    // // i++;

    // int bit = ~number;
    // cout<<bit<<" ";
    // number = number >> 1;
    // }

    //  Decimal to binary in C++
    // int binary = 1111,i = 0,ans = 0;

    // while(binary){

    // int bit = binary % 10;
    // if(bit == 1){
    // ans = ans + pow(2, i);
    // }
    // binary /= 10;
    // i++;
    // }
    // cout<<ans;

    //  We need to create a count of note occurance
    // int amount = 1330,hundredAmount = 0,fiftyAmount = 0,twentyAmount = 0,oneAmount = 0;

    // switch (1)
    // {

    // case 1:
    //  hundredAmount = amount / 100;
    //     amount = amount - hundredAmount * 100;
    // case 2:
    //  fiftyAmount = amount / 50;
    //     amount = amount - fiftyAmount * 50;
    // case 3:
    //  twentyAmount = amount / 20;
    //     amount = amount - twentyAmount * 20;
    // case 4:
    //  oneAmount = amount / 1;
    //     amount = amount - oneAmount * 1;
    // }
    // cout<<"Total Hundred notes required" <<hundredAmount<<endl;
    // cout<<"Total fifty notes required" <<fiftyAmount<<endl;
    // cout<<"Total twenty notes required" <<twentyAmount;
    // cout<<"Total one notes required" <<oneAmount;

    // Binary to decimal in C++
    // int binaryNumber = 1111,ans = 0,i=0;

    //   while(binaryNumber){
    //     int bit = binaryNumber % 10;

    //   if(bit){
    //     ans = ans + pow(2,i);
    //   }
    //   binaryNumber = binaryNumber / 10;
    //   i++;
    //   }
    //   cout<<ans;

    //  Decimal to binary representation in C++
//     int number = 5;
//     int  ans = 0;
//     int i = 0;
//     while(number){

//     int bit = number & 1;
//     ans = (bit * pow(10 , i)) + ans;
//     number = number >> 1;
//    i++;
//     }

//     cout << ans;


    
}